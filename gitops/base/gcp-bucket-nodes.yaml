# GENERATED — do not edit by hand. github-actions[bot] overwrites this on every infra pipeline run.
# Source of truth: infra/gcp-storage/*/ (Terraform). Argo CD syncs whatever is committed here.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: gcp-buckets-pending-sync
  labels:
    platform.dev/provisioned: gcp-storage
data:
  message: |
    Run the Infra Terraform workflow on main (or merge any infra/gcp-storage/* change) to populate
    per-bucket ConfigMaps from terraform output. No bucket names are hardcoded in Git.
