# Software Templates

Register templates in Backstage by setting **`spec.targets`** in `all.yaml` to the **GitHub URLs** of each `template.yaml` in this repository.

Also register `all.yaml` itself in `idp/backstage-values-dev.yaml` under `catalog.locations` (github.com/blob URL).
